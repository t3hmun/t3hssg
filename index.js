const fileLocations = {
  baseDir: `.`,
  intermediateDir: `${baseDir}/intermediate`,
  articleMdDir: `${baseDir}/posts`,
  articleHtmlDir: `${intermediateDir}/articles`,
  articleMetadataFile: `${intermediateDir}/articleMetadata.json`,
};
